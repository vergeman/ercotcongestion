PG_USER=
PG_PASSWORD=
PG_HOST=
PG_PORT=
PG_DATABASE=

# ECR
AWS_PROFILE=
AWS_REGION=
IMAGE_REPO=


# ERCOT
ERCOT_USERNAME=
ERCOT_PASSWORD=
ERCOT_SUBSCRIPTION_KEY=

# Cloudflare-Wrangler-ERCOT Proxy
CLOUDFLARE_API_TOKEN=      # token for wrangler auth and github build pages
CLOUDFLARE_CERT_API_TOKEN= # for DNS cert solver (k3s prod only)
WRANGLER_PROXY_SECRET=     # guard against random requests
PROXY_BASE=                # proxy base url

# API
FRONTEND_ORIGIN=http://localhost:5173
TOPOLOGY_CACHE=/api/static/topology.json
PYTHONPATH=/app

# (set in docker-compose.yml for dev) for prod set Cloudflare Pages env var
#VITE_API_BASE=
