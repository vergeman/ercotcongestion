apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-ercotcongestion-tls
  namespace: ercotstress
spec:
  secretName: api-ercotcongestion-tls
  issuerRef:
    # Your existing cluster-wide issuer.
    name: letsencrypt-ercotstress
    kind: Issuer
  commonName: api.ercotcongestion.com
  dnsNames:
    - api.ercotcongestion.com
  # Renewal kicks in 30d before expiry (cert-manager default).
