# Backfill ingest data from ERCOT for 2025-01-01 → 2026-05-07.
#
# Usage:
#   export IMAGE_REPO=12345.dkr.ecr.us-east-1.amazonaws.com
#   envsubst < ops/deploy/jobs/backfill_ingest_job.yml | kubectl apply -f -
#
#   kubectl -n ercotstress logs -f job/backfill-ingest
#
# This script is idempotent. --resume reads ingest_log and skips any
# (endpoint, day) pairs that already succeeded, so re-running fills only the
# gaps. Adjust --start / --end below to bound a different window.
#
# Expected runtime: hours. ERCOT API is rate-limited; ~493 days × 12 endpoints
# in the worst case (zonal_lmp, shadow, outages, loads, wind, solar, dam_spp,
# dam_shadow, dam_lambda, rt_lmp, sced_lambda, load_forecast). Resume mode means
# most calls will be skipped if your DB already has data. rt_lmp dominates
# (~288K rows/day across all settlement points, paginated 1000/page at a 3s
# throttle).
#
# After it finishes:
#   kubectl -n ercotstress delete job backfill-ingest

apiVersion: batch/v1
kind: Job
metadata:
  name: backfill-ingest
  namespace: ercotstress
  labels:
    app: ercot-ingest
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 86400
  activeDeadlineSeconds: 86400          # 24h hard cap; bump if needed
  template:
    metadata:
      labels:
        app: ercot-ingest
        job: backfill-ingest
    spec:
      restartPolicy: Never
      imagePullSecrets:
        - name: regcred
      containers:
        - name: backfill
          image: ${IMAGE_REPO}/ercotstress/api-compute:${IMAGE_TAG}
          imagePullPolicy: Always
          command:
            - python
            - /ercot_ingest/backfill.py
            - --start
            - "2026-05-01"
            - --end
            - "2026-06-30"
            - --resume
          envFrom:
            - configMapRef:
                name: api-config
            - secretRef:
                name: postgres-credentials
            - secretRef:
                name: ercot-ingest-secrets
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
