plan/
preprocess/
ops/
docs/
db/*.tar.gz

data/raw/*
!data/raw/ercot_geocode

data/experiments/*
!data/experiments/hub_k_sweep
!data/experiments/lambda_validation
!data/experiments/shed_canary

data/runs/*
!data/runs/v1-120
!data/runs/v1-120-postfix

# Run artifacts live on the compute-runs PVC (mounted at /compute/runs), not in
# the image — daily_forecast reads the mu residual pool from there by run_id. Keep
# this ~1.3 GB tree out of the build context.
compute/runs/
